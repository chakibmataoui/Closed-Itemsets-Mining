#pragma once
#include <iostream>
#include <algorithm>
#include <fstream>
#include <string>
#include <vector>
#include <thread>
#include "dsv_filter/dsv_filter.hpp"

using namespace std;

class context {
 public:
  context();
  context(char* context_file);
  vector<vector<bool>> context_matrix;
  int size_i,size_j;
};
